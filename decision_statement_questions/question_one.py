# make a code to compare between two numbers and shows the higher number.

number_one = int(input("Digit a number: "))
number_two = int(input("Digit a number: "))

if number_one > number_two:
    print(f"{number_one} is grater than {number_two}")

else:
    print(f"{number_two} is grater than {number_one}")
