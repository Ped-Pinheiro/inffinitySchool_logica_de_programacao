#Make a code to convert meters for centimeters.

number_meters = int(input('Digit a number to converter meters for contimeters: '))

number_centimenters = number_meters * 100

print(f'The number in centimenters is: {number_centimenters}cm')